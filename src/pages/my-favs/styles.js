import styled from 'styled-components';
import { BOX_SHADOWS } from '../../constants/colors';

export const StyledContainer = styled.div`
	margin-bottom: 3rem;
	border-radius: 0.5rem;
	box-shadow: ${BOX_SHADOWS.small};
`;
